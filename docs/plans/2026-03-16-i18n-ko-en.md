# i18n (한국어 + 영어) 지원 구현 계획

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** mykim.in 블로그에 영어 지원을 추가하여 한국어(기본) + 영어 2개 언어로 서비스

**Architecture:** Route Group `(ko)/`로 기존 한국어 URL 유지, `/en/` prefix로 영어 라우트 추가. middleware에서 언어 감지 및 쿠키 저장. MDX 콘텐츠는 `content/blog/ko/`, `content/blog/en/` 폴더로 분리. UI 텍스트는 JSON 번역 파일로 관리. next-intl 없이 직접 구현.

**Tech Stack:** Next.js 15 App Router, middleware, JSON 번역 파일, gray-matter MDX

---

### Task 1: i18n 설정 파일 생성

**Files:**
- Create: `src/i18n/config.ts`
- Create: `src/i18n/locales/ko.json`
- Create: `src/i18n/locales/en.json`
- Create: `src/i18n/utils.ts`
- Create: `src/i18n/index.ts`

**Step 1: i18n config 생성**

```ts
// src/i18n/config.ts
export const defaultLocale = "ko" as const;
export const locales = ["ko", "en"] as const;
export type Locale = (typeof locales)[number];

export function isValidLocale(locale: string): locale is Locale {
  return locales.includes(locale as Locale);
}
```

**Step 2: 한국어 번역 JSON 생성**

```json
// src/i18n/locales/ko.json
{
  "nav": {
    "series": "Series",
    "tags": "Tags",
    "about": "About",
    "home": "Home",
    "allPosts": "모든 포스트 보기",
    "loading": "로딩 중...",
    "noCategories": "카테고리가 없습니다",
    "theme": "Theme"
  },
  "home": {
    "title": "MYKim",
    "description": "프로그래밍, 웹 개발, 그리고 더 많은 주제에 대한 블로그",
    "posts": "posts"
  },
  "post": {
    "notFound": "포스트를 찾을 수 없습니다",
    "notFoundDesc": "요청하신 포스트를 찾을 수 없습니다."
  },
  "category": {
    "notFound": "카테고리를 찾을 수 없습니다",
    "noPosts": "포스트가 없습니다",
    "noPostsDesc": "카테고리에 아직 포스트가 없습니다.",
    "description": "카테고리 설명",
    "viewOther": "다른 글 보러가기"
  },
  "tags": {
    "title": "Tags",
    "description": "모든 태그를 확인하고 관련 포스트를 찾아보세요.",
    "notFound": "태그를 찾을 수 없습니다",
    "notFoundDesc": "요청하신 태그를 찾을 수 없습니다."
  },
  "about": {
    "title": "About",
    "careerDetail": "Career Detail",
    "content": [
      "저는 어떤 일이든 그 **목적**과 **본질적인 이유**를 고민하는 편입니다.",
      "단순히 실행하는 것보다, **왜 이 작업이 필요한지**, 그리고 그것이 **어떤 의미와 가치를 만들어낼 수 있는지**를 끊임없이 자문하고 정리하는 과정을 중요하게 생각합니다.",
      "팀이 **사용자 경험 중심의 인터페이스**를 정교하게 설계하고 실험하려면, **안정적이고 신뢰할 수 있는 기반(foundation)**이 필요하다고 믿습니다.",
      "삶도 마찬가지입니다. **나를 지탱하는 내면의 기반이 단단할수록**, 더 **깊고 의미 있는 방향**으로 움직일 수 있다고 생각합니다."
    ]
  },
  "career": {
    "title": "Career",
    "description": "경력 및 프로젝트 경험"
  },
  "footer": {
    "copyright": "© {year} {title}. All rights reserved."
  }
}
```

**Step 3: 영어 번역 JSON 생성**

```json
// src/i18n/locales/en.json
{
  "nav": {
    "series": "Series",
    "tags": "Tags",
    "about": "About",
    "home": "Home",
    "allPosts": "View all posts",
    "loading": "Loading...",
    "noCategories": "No categories",
    "theme": "Theme"
  },
  "home": {
    "title": "MYKim",
    "description": "A blog about programming, web development, and more",
    "posts": "posts"
  },
  "post": {
    "notFound": "Post not found",
    "notFoundDesc": "The requested post could not be found."
  },
  "category": {
    "notFound": "Category not found",
    "noPosts": "No posts yet",
    "noPostsDesc": "There are no posts in this category yet.",
    "description": "Category description",
    "viewOther": "Browse other posts"
  },
  "tags": {
    "title": "Tags",
    "description": "Explore all tags and find related posts.",
    "notFound": "Tag not found",
    "notFoundDesc": "The requested tag could not be found."
  },
  "about": {
    "title": "About",
    "careerDetail": "Career Detail",
    "content": [
      "I tend to reflect deeply on the **purpose** and **underlying reason** behind everything I do.",
      "Rather than simply executing tasks, I value the process of asking myself **why this work matters** and **what kind of value it can create**—both for users and for the team.",
      "As a team, we believe that in order to carefully design and experiment with **truly user-centered interfaces**, it's essential to have a **stable and trustworthy foundation in place**.",
      "The same applies to life. When our **inner foundation** is solid, we can move forward with **greater clarity and resilience**."
    ]
  },
  "career": {
    "title": "Career",
    "description": "Career and project experience"
  },
  "footer": {
    "copyright": "© {year} {title}. All rights reserved."
  }
}
```

**Step 4: 번역 유틸리티 함수 생성**

```ts
// src/i18n/utils.ts
import { Locale, defaultLocale } from "./config";
import ko from "./locales/ko.json";
import en from "./locales/en.json";

const dictionaries: Record<Locale, typeof ko> = { ko, en };

export function getDictionary(locale: Locale) {
  return dictionaries[locale] || dictionaries[defaultLocale];
}

// 중첩 키로 번역값 가져오기 (예: "nav.series")
export function t(locale: Locale, key: string, params?: Record<string, string>): string {
  const dict = getDictionary(locale);
  const keys = key.split(".");
  let value: unknown = dict;
  for (const k of keys) {
    if (value && typeof value === "object" && k in value) {
      value = (value as Record<string, unknown>)[k];
    } else {
      return key; // fallback to key
    }
  }
  if (typeof value !== "string") return key;
  if (params) {
    return Object.entries(params).reduce(
      (str, [k, v]) => str.replace(`{${k}}`, v),
      value
    );
  }
  return value;
}

// locale에 따른 URL prefix 반환
export function getLocalePath(locale: Locale, path: string): string {
  if (locale === defaultLocale) return path;
  return `/${locale}${path}`;
}

// pathname에서 locale 추출
export function getLocaleFromPath(pathname: string): Locale {
  const segments = pathname.split("/").filter(Boolean);
  if (segments[0] === "en") return "en";
  return defaultLocale;
}

// locale prefix 제거한 path 반환
export function removeLocaleFromPath(pathname: string): string {
  if (pathname.startsWith("/en")) {
    return pathname.replace(/^\/en/, "") || "/";
  }
  return pathname;
}
```

**Step 5: barrel export 생성**

```ts
// src/i18n/index.ts
export { defaultLocale, locales, isValidLocale } from "./config";
export type { Locale } from "./config";
export { getDictionary, t, getLocalePath, getLocaleFromPath, removeLocaleFromPath } from "./utils";
```

**Step 6: 빌드 확인**

Run: `cd /Users/mykim/Documents/Projects/mykim-blog-front && npx tsc --noEmit --skipLibCheck 2>&1 | head -20`

**Step 7: Commit**

```bash
git add src/i18n/
git commit -m "feat: add i18n configuration with ko/en translations"
```

---

### Task 2: 콘텐츠 폴더 구조 변경 및 MDX 데이터 레이어 수정

**Files:**
- Move: `content/blog/*.mdx` → `content/blog/ko/*.mdx`
- Modify: `content/categories.json` — 카테고리명 한/영 추가
- Modify: `src/lib/mdx/index.ts` — locale 파라미터 추가

**Step 1: content/blog/ko/ 폴더 생성 및 MDX 이동**

```bash
mkdir -p content/blog/ko content/blog/en
mv content/blog/*.mdx content/blog/ko/
```

**Step 2: categories.json 한/영 구조로 변경**

```json
[
  {
    "slug": "logging-mind",
    "name": { "ko": "마음을 로깅합니다", "en": "Logging Mind" },
    "description": { "ko": "생각과 감정을 기록합니다", "en": "Recording thoughts and emotions" }
  },
  {
    "slug": "develop-knowledge",
    "name": { "ko": "지식을 디벨롭합니다", "en": "Developing Knowledge" },
    "description": { "ko": "개발 지식과 기술을 공유합니다", "en": "Sharing development knowledge and skills" }
  },
  {
    "slug": "deploy-life",
    "name": { "ko": "삶을 디플로이합니다", "en": "Deploying Life" },
    "description": { "ko": "삶의 경험을 배포합니다", "en": "Deploying life experiences" }
  },
  {
    "slug": "debug-routine",
    "name": { "ko": "루틴을 디버그합니다", "en": "Debugging Routine" },
    "description": { "ko": "일상의 루틴을 점검합니다", "en": "Debugging daily routines" }
  }
]
```

**Step 3: MDX 데이터 레이어에 locale 파라미터 추가**

`src/lib/mdx/index.ts` 수정:
- `CONTENT_DIR` → `getContentDir(locale)` 함수로 변경
- 모든 함수에 `locale: Locale = "ko"` 파라미터 추가
- `getCategories(locale)` → `name[locale]`, `description[locale]` 반환

주요 변경:

```ts
import type { Locale } from "@/i18n";

function getContentDir(locale: Locale = "ko") {
  return path.join(process.cwd(), "content", "blog", locale);
}

function getAllPostFiles(locale: Locale = "ko") {
  const contentDir = getContentDir(locale);
  // ...기존 로직, CONTENT_DIR → contentDir
}

export function getAllPosts(locale: Locale = "ko"): Post[] { ... }
export function getPaginatedPosts(page, pageSize, locale: Locale = "ko"): ... { ... }
export function getPostBySlugLocal(slug, locale: Locale = "ko"): ... { ... }
export function getCategories(locale: Locale = "ko"): Category[] {
  // name, description을 locale 기반으로 반환
}
export function getPostsByCategory(slug, page, pageSize, locale: Locale = "ko"): ... { ... }
export function getAllTags(locale: Locale = "ko"): Tag[] { ... }
export function getPostsByTag(tagName, page, pageSize, locale: Locale = "ko"): ... { ... }
```

**Step 4: API 래퍼 함수에 locale 전달**

`src/lib/api/server/api.ts` 수정 — 모든 fetch 함수에 locale 파라미터 추가

**Step 5: CMS post-service에 locale 전달**

`src/lib/cms/post-service.ts` 수정 — 모든 함수에 locale 파라미터 추가

**Step 6: 빌드 확인**

Run: `npx tsc --noEmit --skipLibCheck 2>&1 | head -30`

**Step 7: Commit**

```bash
git add content/ src/lib/mdx/ src/lib/api/ src/lib/cms/
git commit -m "feat: restructure content folders for i18n and add locale to data layer"
```

---

### Task 3: Middleware 생성 및 라우트 구조 변경

**Files:**
- Create: `src/middleware.ts`
- Restructure: `src/app/` — route group `(ko)/` 생성, `en/` 폴더 생성

**Step 1: middleware.ts 생성**

```ts
// src/middleware.ts
import { NextRequest, NextResponse } from "next/server";

const locales = ["ko", "en"];
const defaultLocale = "ko";

function getLocaleFromHeaders(request: NextRequest): string {
  const acceptLang = request.headers.get("accept-language");
  if (!acceptLang) return defaultLocale;

  const preferred = acceptLang.split(",").map(l => l.split(";")[0].trim().split("-")[0]);
  return preferred.find(l => locales.includes(l)) || defaultLocale;
}

export function middleware(request: NextRequest) {
  const { pathname } = request.nextUrl;

  // 정적 파일, API, _next 등은 무시
  if (
    pathname.startsWith("/api") ||
    pathname.startsWith("/_next") ||
    pathname.startsWith("/images") ||
    pathname.includes(".") ||
    pathname.startsWith("/rss.xml")
  ) {
    return NextResponse.next();
  }

  // /en/* 경로는 영어 locale 쿠키 설정 후 통과
  if (pathname.startsWith("/en")) {
    const response = NextResponse.next();
    response.cookies.set("locale", "en", { path: "/", maxAge: 60 * 60 * 24 * 365 });
    return response;
  }

  // 그 외 경로는 한국어 기본 — 쿠키 설정
  const response = NextResponse.next();

  // 쿠키에 locale이 없으면 브라우저 언어 감지 후 설정
  const currentLocale = request.cookies.get("locale")?.value;
  if (!currentLocale) {
    const detectedLocale = getLocaleFromHeaders(request);
    response.cookies.set("locale", detectedLocale, { path: "/", maxAge: 60 * 60 * 24 * 365 });

    // 영어 사용자가 루트에 처음 방문하면 /en으로 리다이렉트
    if (detectedLocale === "en" && pathname === "/") {
      return NextResponse.redirect(new URL("/en", request.url));
    }
  }

  return response;
}

export const config = {
  matcher: ["/((?!_next/static|_next/image|favicon.ico).*)"]
};
```

**Step 2: App Router 재구성**

현재 구조:
```
src/app/
  page.tsx
  about/page.tsx
  career/page.tsx
  posts/[slug]/page.tsx
  category/[slug]/page.tsx
  tags/page.tsx
  tags/[slug]/page.tsx
```

변경 후:
```
src/app/
  layout.tsx              ← 루트 레이아웃 (공통)
  (ko)/                   ← Route Group (URL에 안 나옴)
    page.tsx              ← mykim.in/
    about/page.tsx
    career/page.tsx
    posts/[slug]/page.tsx
    category/[slug]/page.tsx
    tags/page.tsx
    tags/[slug]/page.tsx
  en/                     ← mykim.in/en/...
    page.tsx
    about/page.tsx
    career/page.tsx
    posts/[slug]/page.tsx
    category/[slug]/page.tsx
    tags/page.tsx
    tags/[slug]/page.tsx
```

각 `en/` 페이지는 `(ko)/` 페이지를 래핑하는 얇은 레이어 — `locale="en"` prop을 전달.

중복을 줄이기 위해 공유 페이지 컴포넌트를 만듦:
```
src/components/pages/
  HomePage.tsx
  PostPage.tsx
  CategoryPage.tsx
  TagsPage.tsx
  TagDetailPage.tsx
  AboutPage.tsx
  CareerPage.tsx
```

각 `(ko)/page.tsx`와 `en/page.tsx`에서:
```tsx
// (ko)/page.tsx
import HomePage from "@/components/pages/HomePage";
export default function Page() { return <HomePage locale="ko" />; }

// en/page.tsx
import HomePage from "@/components/pages/HomePage";
export default function Page() { return <HomePage locale="en" />; }
```

**Step 3: 루트 레이아웃에서 locale 기반 html lang 설정**

`src/app/layout.tsx` — html lang을 동적으로 설정 (middleware 쿠키 기반)

**Step 4: 빌드 확인**

Run: `npm run build 2>&1 | tail -30`

**Step 5: Commit**

```bash
git add src/middleware.ts src/app/ src/components/pages/
git commit -m "feat: add i18n routing with (ko) route group and /en prefix"
```

---

### Task 4: 헤더 언어 토글 및 컴포넌트 i18n 적용

**Files:**
- Modify: `src/components/layout/Header.tsx` — KO|EN 토글 추가, 번역 키 적용
- Modify: `src/components/layout/InfoCopyRight.tsx` — 번역 적용
- Modify: `src/components/layout/HomePageClient.tsx` — locale prop 추가
- Modify: `src/components/blog/PostDetail.tsx` — locale 기반 텍스트

**Step 1: Header에 언어 토글 추가**

Header 컴포넌트에:
- `locale` prop 추가 (또는 pathname에서 추출)
- 기존 주석 처리된 언어 버튼 활성화
- `KO | EN` 토글: 현재 페이지의 반대 언어 URL로 이동
- 다크모드 토글 옆에 배치
- UI 텍스트를 번역 키로 교체 ("로딩 중..." → `t(locale, "nav.loading")`)
- 카테고리명을 locale 기반으로 표시

**Step 2: InfoCopyRight 번역**

```tsx
// locale에 따라 copyright 텍스트 변경
```

**Step 3: HomePageClient locale 적용**

- `locale` prop 추가
- 링크 경로에 locale prefix 적용 (`/posts/xxx` → `/en/posts/xxx`)

**Step 4: PostDetail locale 적용**

- 카테고리 링크, 태그 링크에 locale prefix
- 날짜 포맷 locale 기반 (ko → "yyyy.MM.dd", en → "MMM dd, yyyy")

**Step 5: 빌드 확인**

**Step 6: Commit**

```bash
git add src/components/
git commit -m "feat: add language toggle to header and apply i18n to components"
```

---

### Task 5: About, Career 페이지 i18n 통합

**Files:**
- Modify: `src/app/about/page.tsx` (→ `src/components/pages/AboutPage.tsx`)
- Modify: `src/app/career/page.tsx` (→ `src/components/pages/CareerPage.tsx`)
- Modify: `src/components/layout/CareerPageClient.tsx`

**Step 1: About 페이지 글로벌 i18n 통합**

- 로컬 `isKorean` state + `toggleLanguage` 제거
- `locale` prop에 따라 `content.ko` / `content.en` 선택
- 콘텐츠를 번역 JSON에서 가져오기

**Step 2: Career 페이지 locale 연동**

- `locale` prop에 따라 `careerData` / `careerDataEn` 선택
- Career 페이지의 기존 한/영 데이터 구조 활용

**Step 3: 빌드 확인**

**Step 4: Commit**

```bash
git add src/app/about/ src/app/career/ src/components/
git commit -m "feat: integrate About and Career pages with global i18n"
```

---

### Task 6: SEO — hreflang, sitemap, RSS, OG 메타

**Files:**
- Modify: `src/app/layout.tsx` — hreflang link 태그
- Modify: `src/app/sitemap.ts` — 언어별 URL 추가
- Modify: `src/app/rss.xml/route.ts` — 영어 RSS 또는 통합
- Modify: `src/app/(ko)/posts/[slug]/page.tsx` — alternates 메타
- Modify: `src/app/en/posts/[slug]/page.tsx` — alternates 메타

**Step 1: 각 페이지 metadata에 hreflang alternates 추가**

```ts
alternates: {
  canonical: `${MAIN.url}/posts/${slug}`,
  languages: {
    "ko": `${MAIN.url}/posts/${slug}`,
    "en": `${MAIN.url}/en/posts/${slug}`
  }
}
```

**Step 2: sitemap에 언어별 URL 추가**

```ts
// 각 포스트에 대해 ko, en 두 개 URL 생성
const postUrls = posts.flatMap(post => [
  { url: `${MAIN.url}/posts/${post.slug}`, ... },
  { url: `${MAIN.url}/en/posts/${post.slug}`, ... }
]);
```

**Step 3: OG locale 메타 — 영어 페이지에서 `locale: "en_US"`**

**Step 4: 빌드 확인**

**Step 5: Commit**

```bash
git add src/app/
git commit -m "feat: add hreflang, sitemap i18n, and OG locale metadata"
```

---

### Task 7: 번역 스크립트 작성 및 18개 글 번역

**Files:**
- Create: `scripts/translate.ts`
- Create: `content/blog/en/*.mdx` (18개)

**Step 1: 번역 스크립트 작성**

```ts
// scripts/translate.ts
// - Claude API로 MDX 파일 번역
// - frontmatter title, description 번역
// - slug, category, tags, featuredImage, publishedDate 유지
// - 본문 마크다운 번역
// - content/blog/en/ 에 저장
```

사용법:
```bash
npx tsx scripts/translate.ts                    # 번역 안 된 파일만
npx tsx scripts/translate.ts --file my-post     # 특정 파일만
npx tsx scripts/translate.ts --force            # 전체 재번역
```

**Step 2: 18개 글 초벌 번역 실행**

**Step 3: 번역 결과 확인 및 저장**

**Step 4: Commit**

```bash
git add scripts/translate.ts content/blog/en/
git commit -m "feat: add translation script and generate English MDX translations"
```

---

### Task 8: 통합 테스트 및 배포

**Step 1: 전체 빌드 확인**

```bash
npm run build
```

**Step 2: 로컬 테스트**

- `mykim.in/` → 한국어 홈
- `mykim.in/en/` → 영어 홈
- `mykim.in/posts/xxx` → 한국어 글
- `mykim.in/en/posts/xxx` → 영어 글
- `mykim.in/about` → 한국어 소개 (글로벌 토글로 전환)
- `mykim.in/en/about` → 영어 소개
- 헤더 `KO | EN` 토글 동작
- 카테고리, 태그 페이지 언어별 확인
- sitemap, RSS 확인

**Step 3: Commit & Push**

```bash
git push origin main
```
